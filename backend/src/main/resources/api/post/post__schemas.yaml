components:
  schemas:
    PostCard:
      type: object
      required:
        - content
        - postedAt
        - isPinned
      properties:
        content:
          type: string
        postedAt:
          type: string
          format: date-time
        isPinned:
          type: boolean
